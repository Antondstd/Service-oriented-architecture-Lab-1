import { ResponsePagesTickets } from './response-pages-tickets.model';

describe('ResponsePagesTickets', () => {
  it('should create an instance', () => {
    expect(new ResponsePagesTickets()).toBeTruthy();
  });
});
